"""BDD step packages — one module per page.

Each submodule is registered as a pytest plugin in
[`tests/conftest.py`](../conftest.py) so its @given/@when/@then
registrations become visible to scenarios.
"""
